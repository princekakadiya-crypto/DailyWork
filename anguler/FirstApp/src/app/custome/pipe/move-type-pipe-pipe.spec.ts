import { MoveTypePipePipe } from './move-type-pipe-pipe';

describe('MoveTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MoveTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
